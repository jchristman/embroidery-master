const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import RenderEmbroideryPattern from '../render_embroidery_pattern';

describe('core.components.render_embroidery_pattern', () => {
  it('should do something');
});
